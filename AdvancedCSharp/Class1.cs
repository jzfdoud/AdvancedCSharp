using System;
using System.Collections.Generic;
using System.Text;

namespace AdvancedCSharp
{
    struct Struct1
    {
        //can have properties, methods, and constructors-but cannot be empty(must have values since it is a value type and must be initialized)

    }
}
